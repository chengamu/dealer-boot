# 05 - 价格、发布与订单快照模块

## 1. 模块目标

这个模块解决：

```text
怎么报价
发布前能不能卖
发布后订单读取哪个版本
订单如何冻结配置、价格、BOM、资料快照
```

## 2. 页面

一期页面：

```text
价格中心
价格试算器
发布检查
审核审批
发布包
同步日志
销售只读总览
订单快照查询
```

## 3. 数据库

优先表：

```text
pc_price_plan
pc_price_plan_version
pc_price_rule_item
pc_publish_check_result
pc_publish_approval
pc_product_publish_package
pc_product_snapshot
pc_product_sync_outbox
```

如果暂时不改名：

```text
pc_product_snapshot_instance 继续承载订单产品能力快照
```

## 4. 后端建议

Controller：

```text
ProductPricingController
ProductPublishController
ProductOrderSnapshotController
```

Service：

```text
ProductPricingService
ProductPublishService
ProductOrderSnapshotService
```

Engine：

```text
PriceCalculationEngine
PublishCheckEngine
ProductSnapshotBuilder
```

## 5. 先做 CRUD

```text
价格方案
价格版本
价格规则项
发布审批
发布包
同步日志
订单快照查询
```

## 6. 必须尽早做试算器 / 检查器

先做：

```text
价格试算器
发布检查器
订单快照构建器
```

价格试算器最小输出：

```text
basePrice
optionAdders
freight
totalPrice
hitRuleItems
warnings
```

发布检查最小检查项：

```text
配置模板已发布
工程模板已发布
价格方案已发布
客户可见资料齐全
组件绑定齐全
BOM 预览可生成
测试用例通过
无 BLOCKER
```

订单快照最小内容：

```text
publishPackageId
templateVersionId
engineeringTemplateVersionId
pricePlanVersionId
selectedOptionsJson
inputValuesJson
configurationSnapshotJson
priceSnapshotJson
bomSnapshotJson
mediaSnapshotJson
snapshotHash
```

## 7. 一期验收

```text
价格能算出命中明细
发布检查能阻止缺配置、缺价格、缺资料、缺 BOM 的产品上线
发布包不可变
订单保存的是快照，不是实时引用草稿
销售只读页只展示发布版本
```
