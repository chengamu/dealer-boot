# 物料属性接口与数据契约

## 页面与权限

| 项目 | 内容 |
| --- | --- |
| 菜单名称 | 基础信息 / 物料属性 |
| 前端路由 | `/product-master/base-attributes` |
| 前端组件 | `product-center/base/ProductBaseInfoPage.vue` 的 `baseAttribute` 配置 |
| 权限码 | `product:base-attribute:*` |

## 后端接口

| 动作 | 方法 | 路径 | 权限 |
| --- | --- | --- | --- |
| 列表 | GET | `/product-capability/base-attributes/list` | `product:base-attribute:list` |
| 选项 | GET | `/product-capability/base-attributes/options` | `product:base-attribute:list` |
| 详情 | GET | `/product-capability/base-attributes/{id}` | `product:base-attribute:list` |
| 修改前检查 | GET | `/product-capability/base-attributes/{id}/edit-check` | `product:base-attribute:edit` |
| 新增 | POST | `/product-capability/base-attributes` | `product:base-attribute:add` |
| 修改 | PUT | `/product-capability/base-attributes` | `product:base-attribute:edit` |
| 启停 | PUT | `/product-capability/base-attributes/change-status/{id}/{status}` | `product:base-attribute:edit` |
| 删除 | DELETE | `/product-capability/base-attributes/{ids}` | `product:base-attribute:remove` |
| 引用检查 | GET | `/product-capability/base-attributes/{id}/references` | `product:base-attribute:reference` |

## 数据表

| 表名 | 用途 |
| --- | --- |
| `pc_base_attribute` | 定义某个属性分组共享的物料属性字段 |
| `pc_material_attribute` | 保存具体物料的属性值 |
| `pc_material_type_group` | 属性分组来源 |

## 关键字段

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `attribute_id` | bigint | 是 | 内部 ID |
| `attribute_group_code` | varchar | 是 | 属性分组编码 |
| `attribute_group_name_cn` | varchar | 否 | 属性分组中文名称快照 |
| `attribute_code` | varchar | 是 | 稳定属性编码 |
| `attribute_name_cn` | varchar | 是 | 中文名称 |
| `attribute_name_en` | varchar | 否 | 英文名称 |
| `value_type` | varchar | 是 | `TEXT` / `NUMBER` / `BOOLEAN` / `ENUM` |
| `unit_code` | varchar | 否 | 默认单位，仅数字属性使用 |
| `status` | varchar | 是 | `ENABLED` / `DISABLED` |
| `sort_order` | int | 否 | 排序 |
| `remark` | varchar | 否 | 备注 |

## 选择器来源

| 页面字段 | 数据来源 | 过滤条件 |
| --- | --- | --- |
| 属性分组 | `pc_material_type_group` | `status = ENABLED` |
| 值类型 | 前端固定枚举 | `TEXT`、`NUMBER`、`BOOLEAN`、`ENUM` |
| 默认单位 | `pc_unit` | `status = ENABLED`；仅值类型为 `NUMBER` 时显示 |

## 可检索性与引用关系

业务编码或单号：

- `attribute_code`

搜索展示字段：

- `attribute_code`
- `attribute_name_cn`
- `attribute_name_en`
- `attribute_group_code`
- `value_type`
- `status`
- `updateTime`

主要引用关系：

- 具体物料属性值通过 `attribute_code` 引用物料属性定义。
- 后续工程规则和配方规则可按属性编码识别结构化属性。

权限过滤依据：

- `tenantId`
- `product:base-attribute:list` / `product:base-attribute:reference`
- `status`
- `delFlag`

详情入口：

| 入口 | 内容 |
| --- | --- |
| 前端路由 | `/product-master/base-attributes` |
| 后端详情接口 | `GET /product-capability/base-attributes/{id}` |

说明：搜索结果只作为入口和摘要展示；属性值类型、启停和引用检查以业务数据库和详情接口为准。

## 校验规则

- `attribute_group_code`、`attribute_code`、`attribute_name_cn`、`value_type` 必填。
- 属性分组必须来自启用的 `pc_material_type_group`。
- 同一属性分组下 `attribute_code` 不能重复。
- `unit_code` 只能在值类型为 `NUMBER` 时填写。
- 删除前必须检查物料属性值和工程规则引用；有引用时优先停用。
- 启用状态资料不能直接编辑，先停用再编辑。

## 请求示例

```json
{
  "attributeGroupCode": "CONTROL",
  "attributeCode": "CHANNEL_COUNT",
  "attributeNameCn": "通道数",
  "attributeNameEn": "Channel Count",
  "valueType": "NUMBER",
  "unitCode": null,
  "status": "ENABLED",
  "sortOrder": 20,
  "remark": "控制系统共享属性"
}
```

## 响应示例

```json
{
  "attributeId": 10001,
  "attributeGroupCode": "CONTROL",
  "attributeGroupNameCn": "控制系统",
  "attributeCode": "CHANNEL_COUNT",
  "attributeNameCn": "通道数",
  "attributeNameEn": "Channel Count",
  "valueType": "NUMBER",
  "unitCode": null,
  "status": "ENABLED",
  "sortOrder": 20
}
```

## 常见错误

| 错误场景 | 后端提示 | 用户提示 |
| --- | --- | --- |
| 属性分组不存在 | `product.baseAttribute.groupNotFound` | 请选择有效的属性分组。 |
| 属性编码重复 | `product.baseAttribute.codeExists` | 当前分组下属性编码已存在，请换一个稳定编码。 |
| 非数字属性填写单位 | `product.baseAttribute.unitOnlyForNumber` | 只有数字型计量属性才需要默认单位。 |
| 删除已引用属性 | `product.baseAttribute.hasReferences` | 该属性已被物料属性或工程规则使用，请先停用。 |

## AI 工具调用契约

### 意图

```text
意图名称：新增物料属性
用户说法：给控制系统分组新增通道数属性，数字类型
适用页面：基础信息 / 物料属性
调用接口：POST /product-capability/base-attributes
权限要求：product:base-attribute:add
```

```text
意图名称：停用物料属性
用户说法：把控制系统里的旧协议字段停用
适用页面：基础信息 / 物料属性
调用接口：PUT /product-capability/base-attributes/change-status/{id}/DISABLED
权限要求：product:base-attribute:edit
```

### 调用接口和权限

| 意图 | 方法 | 路径 | 权限 |
| --- | --- | --- | --- |
| 查询物料属性 | GET | `/product-capability/base-attributes/list` | `product:base-attribute:list` |
| 新增物料属性 | POST | `/product-capability/base-attributes` | `product:base-attribute:add` |
| 编辑物料属性 | GET + PUT | `/product-capability/base-attributes/{id}/edit-check`、`/product-capability/base-attributes` | `product:base-attribute:edit` |
| 启停物料属性 | PUT | `/product-capability/base-attributes/change-status/{id}/{status}` | `product:base-attribute:edit` |
| 查询引用 | GET | `/product-capability/base-attributes/{id}/references` | `product:base-attribute:reference` |

### 必填槽位

- `attributeGroupCode` 或可唯一识别的属性分组名称。
- `attributeCode`：属性编码。
- `attributeNameCn`：属性中文名称。
- `valueType`：值类型，取值 `TEXT`、`NUMBER`、`BOOLEAN`、`ENUM`。

### 可选槽位

- `attributeNameEn`：属性英文名称。
- `unitCode`：默认单位，仅 `NUMBER` 类型可填写。
- `sortOrder`：排序。
- `remark`：备注。

### 可推断字段

- 新增物料属性默认 `status = ENABLED`。
- `attributeGroupNameCn` 由后端根据属性分组回填。
- `unitCode` 不明确时不自动选择。

### 需要追问

- 属性分组、属性编码、属性中文名称或值类型缺失。
- 用户说的属性分组无法唯一匹配。
- 值类型为 `NUMBER` 但是否需要单位不明确。
- 值类型为 `ENUM` 但枚举来源不明确。
- 用户要求编辑启用状态属性，但没有先停用。

### Payload 边界

- AI 不提交 `tenantId`、`createBy`、`createTime`、`updateBy`、`updateTime`、`delFlag`。
- AI 不让用户填写内部 `attributeId`。
- 非 `NUMBER` 类型不提交 `unitCode`。
- AI 不让普通用户直接填写扩展 JSON。
- 编辑前必须先调用 `edit-check`。

### 禁止暴露字段

- `attributeId`
- `tenantId`
- `createBy`
- `createTime`
- `updateBy`
- `updateTime`
- `delFlag`

### 保存前确认格式

```text
将新增物料属性：
- 属性分组：控制系统
- 属性编码：CHANNEL_COUNT
- 属性名称：通道数
- 值类型：数字
- 默认单位：无
- 状态：启用
是否确认保存？
```

### 失败提示

| 场景 | 用户提示 |
| --- | --- |
| 属性分组缺失 | 请说明这个属性属于哪个属性分组。 |
| 属性分组不存在 | 没找到这个属性分组，请先到物料类型维护属性分组。 |
| 属性编码缺失 | 请提供稳定的属性编码。 |
| 属性编码重复 | 当前分组下属性编码已存在，请换一个编码或编辑已有属性。 |
| 值类型缺失 | 请说明值类型，例如文本、数字、开关或枚举。 |
| 非数字属性填写单位 | 只有数字型计量属性才需要默认单位。 |
| 删除已引用属性 | 该属性已被物料属性或工程规则使用，不能删除；可以停用。 |
