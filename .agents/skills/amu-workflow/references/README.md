# AMU Workflow References

本目录存放 amu-workflow 的按需加载资料。

- `rules/`：`/plan`、`/do`、`/archive`、检查、压缩和工具使用规则。
- `templates/`：初始化 .ai 工作区时使用的文档模板。
- `playbooks/`：常见工程场景的可复用经验。

默认不要全量读取本目录；按当前命令和任务风险只读取必要文件。
