<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bocoo.dealer.quickorder.mapper.QuickOrderItemMapper">

    <select id="selectSummaries" resultType="com.bocoo.dealer.quickorder.domain.vo.QuickOrderItemSummaryVo">
        SELECT tenant_id,
            quick_order_id,
            COUNT(DISTINCT sale_product_id) AS item_type_count,
            COALESCE(SUM(quantity), 0) AS total_quantity
        FROM dealer_quick_order_item
        WHERE del_flag = '0'
            AND quick_order_id IN
            <foreach collection="quickOrderIds" item="quickOrderId" open="(" separator="," close=")">
                #{quickOrderId}
            </foreach>
        GROUP BY tenant_id, quick_order_id
    </select>

</mapper>
