# Auth Visual Components

This folder keeps reusable visual pieces for public authentication pages.

- `AuthLocaleSelect.vue`: language selector used by login and merchant application pages.
- `AuthSecureNote.vue`: security reassurance block used by auth cards.

Keep page-specific copy, forms, and backend calls in the page components. Shared items here should stay visual and reusable.
