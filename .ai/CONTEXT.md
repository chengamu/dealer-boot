# Context

## Project Summary：项目摘要

待填写项目背景、技术栈、运行方式和关键约束。

## Important Paths：重要路径

- `D:\work\base-boot`：项目根目录。
- `D:\work\base-boot\AGENTS.md`：项目级 Agent 指令。
- `D:\work\base-boot\.ai`：AMU workflow 状态目录。

## Verification：验证方式

- 待具体任务确认后按相关模块选择 lint/build/test/typecheck。

## Constraints：约束

- 遵守 `AGENTS.md`。
- 不把密钥、Token、连接串写入 `.ai`。
- 不把完整日志写入 `.ai/CURRENT.md`、`.ai/TASKS.md` 或 `.ai/MEMORY.md`。
