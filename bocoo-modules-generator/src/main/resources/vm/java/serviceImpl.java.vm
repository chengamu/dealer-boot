package ${packageName}.service.impl;

import com.baomidou.mybatisplus.core.conditions.query.LambdaQueryWrapper;
import com.baomidou.mybatisplus.core.toolkit.Wrappers;
import com.bocoo.common.core.exception.ServiceException;
import com.baomidou.mybatisplus.core.conditions.update.LambdaUpdateWrapper;
import com.baomidou.mybatisplus.extension.plugins.pagination.Page;
import com.bocoo.common.core.utils.MapstructUtils;
import com.bocoo.common.core.utils.MessageUtils;
import com.bocoo.common.core.utils.StringUtils;
import com.bocoo.common.mybatis.core.page.PageQuery;
import com.bocoo.common.mybatis.core.page.TableDataInfo;
import com.bocoo.common.mybatis.core.service.impl.BaseServiceImpl;
import ${packageName}.domain.bo.${ClassName}Bo;
import ${packageName}.domain.vo.${ClassName}Vo;
import ${packageName}.domain.entity.${ClassName};
import ${packageName}.mapper.${ClassName}Mapper;
import ${packageName}.service.I${ClassName}Service;
import lombok.RequiredArgsConstructor;
import org.springframework.stereotype.Service;

import java.util.Collection;
import java.util.List;
import java.util.Map;

/**
 * ${functionName} service implementation
 *
 * @author ${author}
 */
@Service
@RequiredArgsConstructor
public class ${ClassName}ServiceImpl extends BaseServiceImpl<${ClassName}Mapper, ${ClassName}, ${ClassName}Vo> implements I${ClassName}Service {

    /**
     * Query ${functionName} page list
     *
     * @param bo ${functionName} business object
     * @param pageQuery page parameters
     * @return ${functionName} page data
     */
    @Override
    public TableDataInfo<${ClassName}Vo> queryPageList(${ClassName}Bo bo, PageQuery pageQuery) {
        LambdaQueryWrapper<${ClassName}> lqw = buildQueryWrapper(bo);
        Page<${ClassName}Vo> result = pageVo(pageQuery.build(), lqw);
        return TableDataInfo.build(result);
    }

    /**
     * Query ${functionName} list
     *
     * @param bo ${functionName} business object
     * @return ${functionName} list
     */
    @Override
    public List<${ClassName}Vo> queryList(${ClassName}Bo bo) {
        LambdaQueryWrapper<${ClassName}> lqw = buildQueryWrapper(bo);
        return listVo(lqw);
    }

    /**
     * Query ${functionName} by ID
     *
     * @param id ${functionName} primary key
     * @return ${functionName} information
     */

    public ${ClassName}Vo queryById(Long id) {
        return getVoById(id);
    }

    /**
     * Insert ${functionName}
     *
     * @param bo ${functionName} business object
     * @return whether the operation succeeded
     */
    @Override
    public Boolean insertByBo(${ClassName}Bo bo) {
        ${ClassName} entity = MapstructUtils.convert(bo, ${ClassName}.class);
        validEntityBeforeSave(entity);
        return save(entity);
    }

    /**
     * Update ${functionName}
     *
     * @param bo ${functionName} business object
     * @return whether the operation succeeded
     */
    @Override
    public Boolean updateByBo(${ClassName}Bo bo) {
        ${ClassName} entity = MapstructUtils.convert(bo, ${ClassName}.class);
        validEntityBeforeSave(entity);
        return updateById(entity);
    }

    /**
     * Validate data before saving
     *
     * @param entity ${functionName} entity
     */
    private void validEntityBeforeSave(${ClassName} entity){
        // TODO add validation, such as unique constraints
    }

    /**
     * Delete ${functionName} by ids
     *
     * @param ids ${functionName} primary keys to delete
     * @param isValid whether to validate before deleting
     * @return whether the operation succeeded
     */
    @Override
    public Boolean deleteWithValidByIds(Collection<Long> ids, Boolean isValid) {
        if(isValid){
            // TODO add business validation when needed
        }
        return removeByIds(ids);
    }

    /**
     * Build query criteria
     *
     * @param bo ${functionName} business object
     * @return query criteria
     */
    private LambdaQueryWrapper<${ClassName}> buildQueryWrapper(${ClassName}Bo bo) {
        Map<String, Object> params = bo.getParams();
        LambdaQueryWrapper<${ClassName}> lqw = Wrappers.lambdaQuery();
#foreach($column in $columns)
#if($column.isQuery() && !$column.isSuperColumn())
#if($column.htmlType == "input")
        lqw.like(StringUtils.isNotBlank(bo.get${column.capJavaField}()), ${ClassName}::get${column.capJavaField}, bo.get${column.capJavaField}());
#elseif($column.htmlType == "select" || $column.htmlType == "radio")
        lqw.eq(StringUtils.isNotBlank(bo.get${column.capJavaField}()), ${ClassName}::get${column.capJavaField}, bo.get${column.capJavaField}());
#elseif($column.queryType == "BETWEEN")
        lqw.between(params.get("begin${column.capJavaField}") != null && params.get("end${column.capJavaField}") != null,
            ${ClassName}::get${column.capJavaField}, params.get("begin${column.capJavaField}"), params.get("end${column.capJavaField}"));
#else
        lqw.eq(StringUtils.isNotBlank(bo.get${column.capJavaField}()), ${ClassName}::get${column.capJavaField}, bo.get${column.capJavaField}());
#end
#end
#end
        return lqw;
    }

    /**
     * Update ${functionName} status
     *
     * @param id ${functionName} primary key
     * @param status status value
     * @return updated row count
     */
    @Override
    public int updateStatus(Long id, String status) {
        // Validate parameters
        if (id == null) {
            throw new ServiceException(MessageUtils.message("gen.validation.id.required"));
        }
        if (StringUtils.isEmpty(status)) {
            throw new ServiceException(MessageUtils.message("gen.validation.status.required"));
        }

        // Update only the status field
        return baseMapper.update(null,
                new LambdaUpdateWrapper<${ClassName}>()
                        .eq(${ClassName}::getId, id)
                        .set(${ClassName}::getStatus, status)
        );
    }

}
